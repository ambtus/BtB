-outgoes.sort_by(&:date).reverse_each do |x|
  li
    =display x.amount
    |<> on
    =x.date
    |<> from
    =x.asset.name
    -if x.recipient
      |<> to
      =x.recipient.name
    |<>
    =x.memo
    |<>
    =link_to 'Edit', edit_outgo_path(x)
    |<>
    =link_to 'Delete', delete_outgo_path(x)

-charges.sort_by(&:date).reverse_each do |x|
  li
    =display x.amount
    |<> on
    =x.date
    |<> to
    =x.debt.name
    |<> from
    =x.recipient.try(&:name)
    |<>
    =x.memo
    |<>
    =link_to 'Edit', edit_charge_path(x)
    |<>
    =link_to 'Delete', delete_charge_path(x)

